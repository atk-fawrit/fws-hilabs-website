/**
 * Flagship Program — Feature Types
 */

/** Countdown timer shape returned by useCountdown() */
export interface CountdownTime {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}