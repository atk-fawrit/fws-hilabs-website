import { SqlDataCourse } from '@/src/features/sql-data-foundations/components';

export const metadata = {
  title: 'SQL & Data Foundations | HI Labs Courses',
  description: 'Build a solid grounding in database management, relational algebra, and SQL querying for modern applications.',
};

export default function Page() {
  return <SqlDataCourse />;
}
