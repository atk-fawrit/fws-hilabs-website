import { FrontendReactCourse } from '@/src/features/frontend-developer-react/components';

export const metadata = {
  title: 'Frontend Developer (React) | HI Labs Courses',
  description: 'Learn to build highly interactive, responsive web interfaces using React and modern JavaScript ecosystems.',
};

export default function Page() {
  return <FrontendReactCourse />;
}
